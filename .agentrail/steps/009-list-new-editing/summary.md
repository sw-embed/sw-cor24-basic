Added NEW (clears program store) and line storage/deletion. Inlined store_del into store_ins to save bytes. LIST deferred — the detokenizer exceeds the 8KB monolith limit. Verified multi-unit pipeline works end-to-end: pvm.s loads .p24m, p24-load links units, pa24r assembles v2 format, p24p supports unit mode + forward declarations. The monolith is maxed out at 8.2KB — next step should be the unit split to remove size constraints.