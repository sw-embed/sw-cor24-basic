Write a clean-room Star Trek game specification at docs/startrek-spec.md, based on docs/st24-demo.md. Memory-faithful clone of the 1970s Univac teletype Star Trek game. MUST be a clean-room design from the user's fuzzy memory — do NOT copy from Ahl/Leedom Super Star Trek source or distinctive help text. Spec should define: 8x8 galaxy of 8x8 quadrants; command set (SRS, LRS, WARP, IMP, PHA, TOR, HELP, QUIT, STA, DAM optional); symbols (E=Enterprise, K=Klingon, *=star, B=base, .=empty); SRS/LRS display formats with row/column labels; win/lose conditions (destroy all Klingons before stardate expires); *** RED ALERT *** with BEL when entering Klingon quadrant; ship state (energy, torpedoes, shields, stardate); random galaxy generation. Target the COR24 BASIC v1 subset: A-Z integer variables only, line numbers, GOTO/GOSUB/FOR/NEXT/IF-THEN, PRINT/INPUT, no strings beyond PRINT literals, no arrays beyond what BASIC v1 supports (note: v1 has no arrays — spec must work around this with encoded packed integers or multiple variables). MIT license header.