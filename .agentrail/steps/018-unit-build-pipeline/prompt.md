Refactor the build pipeline for p-code unit compilation. Update tests/toolchain/run.sh to support multi-unit builds: each src/*.pas compiles to a .spc unit, assembled independently to .p24 (v2 with exports/imports), then combined via p24-load into a single .p24m image. Each module declares .export for its public procedures and .import/.extern for cross-unit calls. The p24p compiler needs to emit .unit/.export directives (or we post-process the .spc). Create a justfile or build.sh that: (1) compiles each module, (2) links each unit with runtime, (3) assembles each to .p24, (4) loads all units into final image. Test with basic_tokens + basic_lex as two separate units calling each other.