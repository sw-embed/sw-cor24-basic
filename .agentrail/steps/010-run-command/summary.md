Added RUN command via main-loop run-flag that copies stored lines into tb[] and dispatches sequentially. Trimmed ABS and unused SAVE/LOAD/PEEK/POKE keywords to fit the ~8KB UART monolith limit. Also added 4 Star Trek demo saga steps (021-024: spec, feature audit, impl, validate) per docs/st24-demo.md.