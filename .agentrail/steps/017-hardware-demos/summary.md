Added PEEK/POKE wired to p24p peek/poke runtime builtins (FK+21 in p_expr level 0; FK+22 dispatch case). New demos: count (FOR/NEXT smoke), memdump (POKE+PEEK round-trip on low scratch addrs), blink (LED 0xFF0000 toggle - hardware-only, pv24t traps). docs/demos.md and README updated with current v1 status, build/run instructions, and the pv24t-vs-hardware divide for MMIO. Source 12401 bytes.