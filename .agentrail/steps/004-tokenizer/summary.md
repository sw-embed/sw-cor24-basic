Implemented tokenizer (src/basic_lex.pas): keyword matching (24 keywords, longest-match, case-insensitive), integer literals (24-bit), string literals, variables A-Z, operators including two-char <> <= >=, delimiters, line number extraction, REM rest-of-line. 3 smoke tests pass. Discovered UART input ~3.5KB limit for p24p compilation; kept source compact. Updated run.sh to use -u flag with 8K stack.