Split src/basic.pas monolith into separate unit source files that compile independently. Each module (basic_tokens, basic_lex, basic_store, basic_expr, basic_stmt, basic_io, basic_main) becomes a .pas file that compiles to its own .p24 unit. Add .export annotations for public procedures and .import/.extern for cross-unit dependencies. Verify each unit compiles under 8KB. Build the full interpreter from units via the multi-unit pipeline. Test that PRINT, LET, line storage, and BYE all work end-to-end.