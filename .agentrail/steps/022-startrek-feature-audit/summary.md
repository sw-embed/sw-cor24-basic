Wrote docs/startrek-feature-matrix.md auditing all 40 BASIC v1 features required by the Star Trek spec. 19/19 statements OK, 11/11 operators OK, 3/4 I/O primitives OK, 6/6 game-design helpers worked around in spec. ONE real gap: BEL emission for the red alert (no CHR$, no embedded control bytes in PRINT literals). Added saga step 026-print-chr-builtin (minimal CHR$(n) recognized only inside PRINT). Verified during audit: I-(I/N)*N modulo, nested GOSUB, IF...THEN GOTO all work as assumed. Step 023 (impl) otherwise unblocked.