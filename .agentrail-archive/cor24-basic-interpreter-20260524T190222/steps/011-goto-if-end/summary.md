Added GOTO/IF-THEN/END/STOP via re-entry loop in dispatch. GOTO reuses store_find; IF uses p_expr(4) then re-dispatches statement after THEN on true. Trimmed 8 unused keyword entries to fit 8KB monolith; source now 8103 bytes.