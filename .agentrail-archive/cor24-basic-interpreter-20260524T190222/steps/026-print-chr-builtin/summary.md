Added CHR$(n) as a PRINT-only builtin. Tokenizer recognizes CHR$ as keyword 24; do_print handles FK+24 by parsing '(' expr ')' and emitting the byte via pc(chr(ev)). Updated examples/startrek.bas red-alert lines to use PRINT CHR$(7);"..." and rewrote docs/startrek-spec.md §7/§10.3 to drop the POKE-to-UART workaround. Verified: PRINT CHR$(65);CHR$(66);CHR$(67) prints ABC; PRINT CHR$(7) emits literal BEL. Existing hello/fizzbuzz/fibonacci demos still pass. Bookkeeping catch-up for steps 024-026 included.