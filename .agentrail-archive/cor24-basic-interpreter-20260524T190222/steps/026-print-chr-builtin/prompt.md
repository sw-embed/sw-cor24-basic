Add CHR$(n) recognized inside PRINT item lists. When do_print sees tb[ep]=FK+24 it consumes '(', parses the inner expression with p_expr(4), requires ')', and emits the byte via pc(chr(ev)) instead of routing through print_int. Add ks(24,'C','H','R','$',' ',' ') in ik. No CHR$ outside PRINT (an error there is fine - keeps the change minimal). Test: PRINT CHR$(65);CHR$(66);CHR$(67) produces ABC; PRINT CHR$(7) produces a literal BEL byte. Estimated ~120 source bytes. After this ships, update docs/startrek-spec.md section 7 to use PRINT CHR$(7) for the red alert instead of the POKE-to-UART placeholder.