Validate examples/startrek.bas end-to-end on the emulator. Run a scripted playthrough (deterministic seed) that exercises every command in the spec. Capture UART output to tests/startrek-transcript.txt and compare against a golden transcript. Verify: (1) game starts and prints banner, (2) SRS/LRS render correctly, (3) WARP between quadrants works and triggers red alert, (4) combat (PHA, TOR) destroys Klingons and updates energy/torpedoes, (5) win path reachable, (6) lose path (out of energy or stardate) reachable. Document any flakiness or interpreter bugs found; file fixes as follow-up steps. Update README.md with a 'Star Trek demo' section pointing to examples/startrek.bas and play instructions.