Audit BASIC interpreter features required by docs/startrek-spec.md. Produce docs/startrek-feature-matrix.md listing each required BASIC feature (statements, operators, functions, I/O) vs current interpreter support. For any missing/untested features, file them as additional saga steps (use 'agentrail add') before the startrek-impl step. Known v1 constraints: no arrays, A-Z scalars only, integer only. Verify: RND or equivalent random-number source (may need new builtin), INT/ABS, modulo via DIV, string INPUT vs numeric INPUT, BEL character (chr(7)) via PRINT, multi-statement lines (if supported), IF...THEN with GOTO, nested GOSUB, FOR/NEXT with negative STEP. For each gap, either (a) add an implementation step, or (b) document a workaround in the spec. Update docs/startrek-spec.md with any workarounds.