Fix the ABS function bug (sw-embed/sw-cor24-basic#1). ABS(expr) currently falls through to variable parsing because slot 23 isn't initialized in the ik procedure. Fix: (1) add ks(23,'A','B','S',' ',' ',' ') in ik; (2) in p_expr level 0 add a case for tb[ep]=FK+23 that consumes ABS, expects '(' (token 176), recursively parses the inner expression, takes the absolute value (if v<0 then v:=0-v), and expects ')' (token 177). Verify: ABS(-42)=42, ABS(42)=42, ABS(0)=0, ABS(A-B) evaluates inner first, and ./scripts/demo-calc.sh shows ABS(-42)=42. Close issue #1 when done.